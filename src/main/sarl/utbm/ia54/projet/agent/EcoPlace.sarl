package utbm.ia54.projet.^agent

import io.sarl.core.DefaultContextInteractions
import io.sarl.core.Initialize
import io.sarl.util.Scopes
import java.util.UUID
import utbm.ia54.projet.message.UUIDEvent

/**
 * @author Lucas
 *
 */
agent EcoPlace extends EcoAgent {
	uses DefaultContextInteractions
	
	on Initialize {
		num = occurrence.parameters.get(0) as Integer
		println(num);
		idLinked = occurrence.parameters.get(1) as UUID
		println(ID + "\t" + idLinked)
		emit(new UUIDEvent(ID), Scopes.addresses(defaultSpace.getAddress(idLinked)))
		killMe
	}
}